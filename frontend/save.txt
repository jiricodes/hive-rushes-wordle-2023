// const gridContainer = document.querySelector('.grid-container');
// let selectedBox = null;

// // Get all box elements
// const boxElements = document.querySelectorAll('.box');

// // Click event for each box
// boxElements.forEach((box) => {
//   box.addEventListener('click', () => {
//     if (selectedBox) {
//       selectedBox.classList.remove('selected');
//     }

//     selectedBox = box;
//     selectedBox.classList.add('selected');
//     selectedBox.querySelector('input').focus();
//   });
// });

// // Mouseover and Mouseout effects for each box
// boxElements.forEach((box) => {
//   box.addEventListener('mouseover', () => {
//     if (box !== selectedBox) {
//       box.classList.add('hover');
//     }
//   });

//   box.addEventListener('mouseout', () => {
//     if (box !== selectedBox) {
//       box.classList.remove('hover');
//     }
//   });
// });

// // Input event
// gridContainer.addEventListener('input', (e) => {
//   const input = e.target;
//   const letter = input.value;
//   input.previousElementSibling.textContent = letter.toUpperCase();
// });

// // Backspace event
// gridContainer.addEventListener('keydown', (e) => {
//   if (e.key === 'Backspace' && selectedBox) {
//     const input = selectedBox.querySelector('input');
//     input.value = '';
//     input.previousElementSibling.textContent = '';
//   }
// });
//   const keyboardButtons = document.querySelectorAll('.keyboard button');
//   const boxes = document.querySelectorAll('.box');

//   // Set the currently selected box to the first box
//   let selectedBox = boxes[0];
  
//   // Helper function to get the index of a box in the grid
//   function getBoxIndex(box) {
//     return Array.from(boxes).indexOf(box);
//   }
  
//   // Function to move the cursor to the next box when a letter is filled
//   function moveCursorToNextBox() {
//     const currentIndex = getBoxIndex(selectedBox);
//     const nextIndex = currentIndex + 1;
  
//     if (nextIndex < boxes.length) {
//       selectedBox = boxes[nextIndex];
//       selectedBox.querySelector('input').focus();
//     }
//   }
  
//   // Function to move the cursor to the previous box
//   function moveCursorToPreviousBox() {
//     const currentIndex = getBoxIndex(selectedBox);
//     const previousIndex = currentIndex - 1;
  
//     if (previousIndex >= 0) {
//       selectedBox = boxes[previousIndex];
//       selectedBox.querySelector('input').focus();
//     }
//   }
  
//   // Click event for each keyboard button
//   document.addEventListener('keydown', (event) => {
//     if (event.key.length === 1) {
//       // If a letter key is pressed, fill the selected box with the letter
//       selectedBox.querySelector('input').value = event.key.toUpperCase();
//       selectedBox.querySelector('span').textContent = event.key.toUpperCase();
  
//       // Move the cursor to the next box
//       moveCursorToNextBox();
//     } else if (event.key === 'ArrowLeft') {
//       // If the left arrow key is pressed, move the cursor to the previous box
//       moveCursorToPreviousBox();
//     } else if (event.key === 'ArrowRight') {
//       // If the right arrow key is pressed, move the cursor to the next box
//       moveCursorToNextBox();
//     }
//   });
//   const deleteButton = document.getElementById('delete-button');

// // Click event for the delete button
// deleteButton.addEventListener('click', () => {
//   if (selectedBox) {
//     selectedBox.querySelector('input').value = '';
//     selectedBox.querySelector('span').textContent = '';
//   }
// });
// });